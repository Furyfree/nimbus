[terminal]
vt = 1

[general]
# Skip shell profiles for all sessions so a broken ~/.profile cannot block
# recovery. Interactive shells in the normal session load their own config.
source_profile = false

[default_session]
command = "/usr/bin/noctalia-greeter-session"
user = "greetd"
