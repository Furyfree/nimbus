# Tailscale from Fedora. Authentication remains a manual task.
schema = 1
id = "tailscale"

requires = []
conflicts = []
packages = ["tailscale"]
removes = []

[[services]]
unit = "tailscaled.service"
enabled = true
running = true
