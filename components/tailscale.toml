# Tailscale from Fedora. Authentication remains a manual task.
# nimbus postinstall tailscale-operator offers local operator permission.
schema = 1
id = "tailscale"

requires = []
conflicts = []
packages = ["tailscale"]
removes = []

[[services]]
unit = "tailscaled.service"
enabled = true
running = true
