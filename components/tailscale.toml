# Tailscale from Fedora. Explicit postinstall guides native sign-in when needed.
# nimbus postinstall tailscale-operator verifies sign-in and operator permission.
schema = 1
id = "tailscale"

requires = []
conflicts = []
packages = ["tailscale"]
removes = []

[[services]]
unit = "tailscaled.service"
enabled = true
running = true
