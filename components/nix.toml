# Nix from Fedora with its multi-user daemon enabled and running.
schema = 1
id = "nix"

requires = []
conflicts = []
packages = ["nix", "nix-daemon"]
removes = []

[[services]]
unit = "nix-daemon.service"
enabled = true
running = true
