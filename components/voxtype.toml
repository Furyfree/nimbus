# Install the packaged build with x86-64-v3 kernels and the Vulkan backend;
# nimbus postinstall voxtype downloads the model and enables the user unit.
schema = 1
id = "voxtype"
requires = []
conflicts = []
packages = ["voxtype:voxtype", "wtype", "wl-clipboard", "libnotify", "pipewire-alsa"]
removes = []
