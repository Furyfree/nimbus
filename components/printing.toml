# Printing and scanning. The Epson ESC/P-R drivers wait for a package source.
schema = 1
id = "printing"

requires = []
conflicts = []
packages = [
  "cups",
  "avahi",
  "ghostscript",
  "sane-airscan",
  "simple-scan",
]
removes = []

[[services]]
unit = "cups.socket"
enabled = true
running = true

[[services]]
unit = "cups.path"
enabled = true
running = true

[[services]]
unit = "avahi-daemon.service"
enabled = true
running = true
