# Optional TPM automatic disk unlock: a Unified Kernel Image and, in the next
# milestone, its signature and a scoped systemd-cryptenroll keyslot. The
# postinstall task inspects read-only first; setup, enrollment, renewal and
# removal stay explicit and never run from sync.
schema = 1
id = "fde"

requires = []
conflicts = []

packages = [
  "systemd-ukify",
  "sbsigntools",
  "efibootmgr",
]

removes = []
