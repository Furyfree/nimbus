# Laptop brightness and fingerprint reader, with shared power management.
schema = 1
id = "laptop-power"

requires = ["power-management"]
conflicts = []
packages = [
  "brightnessctl",
  "fprintd",
]
removes = []

[detect]
chassis = "laptop"
